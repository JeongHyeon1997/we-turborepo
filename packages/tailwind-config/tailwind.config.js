/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      // 공유 디자인 토큰을 여기에 정의하세요
      // colors, fontFamily, spacing 등
    },
  },
  plugins: [],
};
